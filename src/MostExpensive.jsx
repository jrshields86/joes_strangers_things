const MostExpensive = ()=> {
    return (
      <div className="most">
        <h3>Most Expensive</h3>
        <ul>
        {
        //   Posts.filter((post)=>{
        //     const most = `Math.max(${post.price})`
        //   })
        
        }
        </ul>
      </div>
    );
  };
  
  export default MostExpensive;